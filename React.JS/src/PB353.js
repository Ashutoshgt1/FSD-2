//having an arrayofstring and convert it in upper case
//  using map method
const Convert =()=>{
    const arr = ['jhcb']
    return(<div>
        <h1>using Arraymap function</h1>
        { arr.map((value)={return<h1>array values ={valus.toUpperCase()}</h1>
        })
    </div>)
}
